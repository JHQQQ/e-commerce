<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mitsuha754.ecommerce.mapper.OrderItemMapper">
    <insert id="addOrderItem">
        INSERT INTO order_item (
        order_no, product_id, product_name, product_image, price, quantity, total_amount, username
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{orderNo},
            #{item.productId},
            #{item.productName},
            #{item.image},
            #{item.price},
            #{item.quantity},
            #{item.totalPrice},
            #{userName}
            )
        </foreach>
    </insert>

    <select id="getOrderItems" resultType="com.mitsuha754.ecommerce.entity.OrderItem">
        SELECT
               id,
               product_id AS productId,
               product_name AS productName,
               product_image AS image,
               price,
               quantity,
               total_amount AS totalPrice,
               order_no AS orderNo,
               create_time AS createTime
        FROM order_item
        WHERE order_no = #{OrderNo}
    </select>
</mapper>